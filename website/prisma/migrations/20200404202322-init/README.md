# Migration `20200404202322-init`

This migration has been generated by bjoernRave at 4/4/2020, 8:23:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Website" (
    "baseUrl" text  NOT NULL ,
    "id" text  NOT NULL ,
    "logo" text   ,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Product" (
    "currency" text  NOT NULL ,
    "description" text   ,
    "id" text  NOT NULL ,
    "images" text []  ,
    "name" text  NOT NULL ,
    "price" Decimal(65,30)  NOT NULL ,
    "url" text  NOT NULL ,
    "websiteId" text   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Probability" (
    "className" text  NOT NULL ,
    "id" text  NOT NULL ,
    "productId" text  NOT NULL ,
    "value" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id")
) 

ALTER TABLE "public"."Product" ADD FOREIGN KEY ("websiteId")REFERENCES "public"."Website"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Probability" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200404202322-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Website {
+  id       String       @default(cuid()) @id
+  baseUrl  String
+  name     String
+  products Product[]
+  logo     String?
+}
+
+model Product {
+  id            String           @default(cuid()) @id
+  url           String
+  images        String[]
+  name          String
+  price         Float
+  currency      String
+  description   String?
+  probabilities Probability[]
+  websiteId     String?
+  website       Website?      @relation(fields: [websiteId], references: [id])
+}
+
+model Probability {
+  id        String     @default(cuid()) @id
+  className String
+  value     Float
+  productId String
+  product   Product @relation(fields: [productId], references: [id])
+}
```


